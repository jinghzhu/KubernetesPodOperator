language: go

os:
  - linux
  - osx

# before_install:
#   - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update          ; fi

go:
  - 1.10
  - master

install:
  - go get -d -v ./...

script: echo "hello"
